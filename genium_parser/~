function parse(b, l) {
      
  var messageGroup = b.readInt16LE(0);
  var messageId= b.readInt16LE(2);

  var msgKey=(messageId << 16) | messageGroup;
  switch(msgKey) {
    case 1114122:
      // account
      util.log("account");
      l.onAccount(b);
      break;
    case 589834:
      // start of trx
      util.log("startOfTransaction");
      l.onStartOfTransaction(b);
      break;
    case 458762:
      // trade
      util.log("trade");
      l.onTrade(b);
      break;
    case 196618:
      // user
      util.log("user");
      l.onUser(b);
      break;
    case 131082:
      // participant
      util.log("participant");
      l.onParticipant(b);
      break;
    case 655370:
      // commit
      util.log("commit");
      l.onCommit(b);
      break;
    case 65546:
      // orderbook
      util.log("orderbook");
      l.onOrderBook(b);
      break;
  }
      
}

module.exports = {
  parser: parse 
}
